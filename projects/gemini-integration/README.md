# Gemini Integration

Wire up the `gemini-design` MCP server and validate that `execute_task` can process YouTube URLs reliably.

**Status:** Active
**Goal:** End-to-end 2–3 phase extraction flow working on any public YouTube URL
